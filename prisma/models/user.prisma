model User {
  id String @id @default(uuid())

  name     String
  email    String @unique
  password String
  role     Role   @default(USER)
  posts    Post[]
  news     News[]

  createdAt DateTime @default(now())

  @@map("users")
}

enum Role {
  USER
  ADMIN
  MODERATOR
}
